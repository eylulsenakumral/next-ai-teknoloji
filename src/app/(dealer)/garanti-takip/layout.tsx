import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Garanti Takip",
}

export default function GarantiTakipLayout({ children }: { children: React.ReactNode }) {
  return children
}
